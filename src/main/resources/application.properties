# Enable debug WebSocket actions (set to false for production)
app.debug=true

# Game state persistence (JSON file per game)
app.persistence.enabled=true
app.persistence.dir=data

# Turn timeout: auto-finish a player's turn if they are idle this many seconds.
app.turn-timeout-seconds=60

